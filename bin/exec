#!/usr/bin/env sh

echo "$@"
if sh -c ": >/dev/tty" >/dev/null 2>/dev/null; then
    docker-compose run --rm --service-ports app "$@"
else
    echo "No TTY - running with -T"
    docker-compose run --rm --service-ports -T app "$@"
fi